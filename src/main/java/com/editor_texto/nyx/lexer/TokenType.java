package com.editor_texto.nyx.lexer;

public enum TokenType {
    KEYWORD,
    TYPE,
    IDENTIFIER,
    NUMBER,
    HEX,
    STRING,
    BOOLEAN_LITERAL,
    OPERATOR,
    DELIMITER,
    COMMENT,
    ERROR,
    WHITESPACE
}
